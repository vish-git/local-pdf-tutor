import {useEffect,useState} from 'react';
import type {FormEvent} from 'react';
import './styles.css';
const API='http://localhost:8000';
type Doc={document_id:string;filename:string;title:string;pages:number;records:number;extraction_mode:string;status:string};type Image={page:number;url:string};type Block={heading?:string|null;text:string};type Source={document_id:string;question_number?:number|null;question:string;text:string;answer:string;answer_blocks:Block[];page?:number|null;page_start?:number|null;page_end?:number|null;page_images:Image[];score:number;match_type:string};
export default function App(){const[docs,setDocs]=useState<Doc[]>([]),[did,setDid]=useState(''),[q,setQ]=useState(''),[ans,setAns]=useState(''),[sources,setSources]=useState<Source[]>([]),[busy,setBusy]=useState(false),[msg,setMsg]=useState('');const load=async()=>{const r=await fetch(API+'/api/documents');const d=await r.json();setDocs(d);if(!did&&d.length)setDid(d[0].document_id)};useEffect(()=>{load().catch(e=>setMsg(e.message))},[]);const upload=async(f:File)=>{setBusy(true);setMsg('');try{const fd=new FormData();fd.append('file',f);const r=await fetch(API+'/api/documents/upload',{method:'POST',body:fd});const d=await r.json();if(!r.ok)throw Error(d.detail);await load();setDid(d.document_id);setMsg(`${d.filename} is ready.`)}catch(e){setMsg(e instanceof Error?e.message:'Upload failed.')}finally{setBusy(false)}};const ask=async(e:FormEvent)=>{e.preventDefault();if(!did||!q.trim())return;setBusy(true);setAns('');setSources([]);try{const r=await fetch(API+'/api/doubt',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({document_id:did,question:q})});const d=await r.json();if(!r.ok)throw Error(d.detail);setAns(d.answer);setSources(d.sources)}catch(e){setMsg(e instanceof Error?e.message:'Question failed.')}finally{setBusy(false)}};return <div className="app"><header><h1>Local PDF Tutor</h1><p>Multiple PDFs • Local search • No cloud</p></header><main><section className="card"><h2>Upload PDF</h2><input type="file" accept="application/pdf,.pdf" disabled={busy} onChange={e=>{const f=e.target.files?.[0];if(f)upload(f)}}/>{msg&&<p className="msg">{msg}</p>}</section><section className="card"><h2>Ask a Question</h2>{docs.length?<><select value={did} onChange={e=>{setDid(e.target.value);setAns('');setSources([])}}>{docs.map(d=><option key={d.document_id} value={d.document_id}>{d.filename}</option>)}</select><textarea rows={5} value={q} onChange={e=>setQ(e.target.value)} placeholder="Ask a question about the selected PDF..."/><button disabled={busy} onClick={ask}>{busy?'Working...':'Ask'}</button></>:<p>Upload a PDF first.</p>}</section>{ans&&<section className="card"><h2>Answer</h2><div className="answer">{ans}</div>{sources.map((s,i)=><article className="source" key={i}><h3>Source {s.question_number?`— Question ${s.question_number}`:''}</h3>{s.question&&<p>{s.question}</p>}<p className="muted">Pages {s.page_start===s.page_end?s.page_start:`${s.page_start}–${s.page_end}`}</p>{s.answer_blocks.map((b,j)=><div key={j}>{b.heading&&<h4>{b.heading}</h4>}<p>{b.text}</p></div>)}<div className="pages">{s.page_images.map(im=><figure key={im.page}><img src={API+im.url} alt={`PDF page ${im.page}`}/><figcaption>PDF page {im.page}</figcaption></figure>)}</div></article>)}</section>}</main></div>}
